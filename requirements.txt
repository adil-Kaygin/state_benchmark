# Core framework dependencies (mirrors setup.py install_requires).
numpy>=1.24
h5py>=3.8
torch>=2.0
matplotlib>=3.7

# Used by notebooks/experiment_*.py for the results table.
pandas>=2.0

# Optional accelerator for KF/UKF linear fast paths (estimators/classical/_numba_kernels.py).
# Falls back to pure NumPy loops when not installed.
numba>=0.58

# Optional: enables metrics.memory.measure_memory(); returns None without it.
psutil>=5.9

# Dev / testing.
pytest>=7.0
